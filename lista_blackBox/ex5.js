// Exercício 5: Inverter lista (nova lista invertida)
// TODO: Crie nova array com elementos na ordem inversa

let lista = []; // preencha
let invertida = [];
// seu código...

document.getElementById('resultado').innerHTML = `Original: [${lista}], Invertida: [${invertida}]`;
console.log('Invertida:', invertida);
